How to approach my learning ?
I fix a goal (now that robot is in the simulation, my goal is the make to robot drive). So I will go and learn / get the required ressource to achieve that goal.


*** diff drive plugin ***
After have added the diff drive plugin to gazebo, don't forget to add bridge in the launch file.
To test diff_drive, we will use: 'ros2 run teleop_twist_keyboard teleop_twist_keyboard'

** How to solve the friction problem in the wheel so that they can move ? **

The robot drive will be like **Inverted Pendulum**
I learn that diff_drive is for car setup only

=== Rolling Testing ===
Issue on backward rolling  ??? The robot hit bend due to inertia effects after have applied driving force.
The legs band because they are in "revolute" without motors (porte colissante) but by seting motors, we can maintain
their position fixed to a certain degree.
Note that at this stage, I have only two motors in wheels.
So to fixed the problem I have to move from using pluggins (like diff_drive) to ros2_controller ?

== What is ros2_controller ? ==
Note that diff_drive is a controller, but it is limited in the system. When I will add motor to the legs, I will need
another controller to control them (something like joint-bending controller in ros2, it called joint_trajectory_controller).

ros2_controller is a framework which allows us to organize our project in a certain way so that the code written 
for the simulation will completly work the real world. Because it is well organize, it allows us to manage different
controllers in the project.

It is separate in 3 levels:
 ** Controllers ** : Intead of using gazebo controller (diff_drive), we will use ros2_diff_drive_controller to 
 do the work. I will allow ros2 to manage motor intead of gazebo and ros2 can manage multiple controller at the
 same time and check if their are working well. the controllers will be defined in a .yaml file.

 ** Controller Manager **: it takes the mathematical output from the controller and make sure that there is not 
 conflit between ctrls.

 ** Hardware Interface **: it translate number into electronic signal. It here we will specify the motors
 of our robot.  To communicate with gazebo to send cmd, we use the plugin 'gz_ros2_control'. The hardware Interface
 is written in an urdf file.

## ros2 control commands
'ros2 control list_hardware_interfaces'
'ros2 control list_controllers'
'ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args -r /cmd_vel:=/diff_drive_controller/cmd_vel_unstamped'


Driving interface: sudo apt install ros-jazzy-rqt-robot-steering

## rqt_robot_steering
rqt_robot_steering is a simple tool that publishes the basic Twist. And to allow ros2_control to hangle twist data,
we have to set in the .yaml file use_time_stamp: false so that it could parse twist data.


## Walking Mode
to switch from walking to Rolling mode I have to change:
left_hip_joint:  rpy="0 0 0"  rpy="1.5708 0 0"
right_hip_joint: rpy="0 0 0"  rpy="-1.5708 0 0"

In <ros2_control>:  <command_interface name="velocity"/> =>  <command_interface name="position"/>
Note: You can check if the controller (brain) is active or note via: ros2 control list_controllers
Note: You will often deal with .yaml file, the launch file and the urdf file (ros2_control), If you have an Issue,
think to check those 3 files.

# == Testing Walking with the second brain (joint_trajectory_controller from ros2_control) ==
I will write a python script where I made the robot perform forward walk.


##======== CONFLICTS =======
I am facing a conflict here. I want two brains (two controllers: diff_drive & joint_trajectory) to controller same joints
(left_foot_joint & right_foot_joint) which is a violation rule in ros2. So it will not work.

How to fix that conflict ?
Keep one brain  (in .yaml file) on the share joints. This approach will allow me to test the walking. Later, I will look for a 
way to keep both and transition from walking to rolling and vice-versa.
 ros2 run otto_description otto_walk.py

otto_walk contains a script which publishes in the topic /joint_trajectory_controller/joint_trajectory joints.

------------
Now that I have tested and validate both rolling and walking individualy, next time, I will work a way to 
use them in serie where I could switch dynamically from rolling to walking and vice-versa without changing manually
parameters and by respecting the rule of 'Not multiple brain/controller on the same joints.'

